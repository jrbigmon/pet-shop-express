<!DOCTYPE html>
<html lang="en">
<link rel="stylesheet" href="/css/service.css">
<%- include('../partials/head')  %>

<body>

    <%- include('../partials/header')  %>

    <main>
        <h1 id="service-title"> SERVIÇOS </h1>
        <% servicos.forEach(servico => { %>
        <section class="section section-1">
            <img class="section-img" src="<%= servico.img %>" alt="">

            <div class="section-describ">
                <span class="describ-title"> <%= servico.name %> </span>

                <span class="describ-valor"> <%= servico.valor %> </span>
                <p> <%= servico.describ %> </p>

                <a href="#"> agendar </a>
            </div>
        </section>
        <% }) %>
    </main>

    <%- include('../partials/footer') %>

</body>

</html>